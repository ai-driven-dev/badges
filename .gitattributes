# Photos des membres -> Git LFS (CT-12). Un dossier par membre :
# data/members/<handle>/photo.webp. Le binaire reste hors de l'historique
# principal ; l'effacement RGPD est `rm -rf data/members/<handle>/`.
data/members/*/photo.webp filter=lfs diff=lfs merge=lfs -text
